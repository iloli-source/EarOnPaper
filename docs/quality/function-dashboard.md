# 機能品質ダッシュボード

**更新:** 2026-07-19（機能別ダメ出し第1巡の完了時点）
**規約:** function-critique-protocol.md の完成条件(b)。スコアは実測数字のみ（推測記入禁止）。指標はAIの耳 **v2**（v1数字と比較不可）。

## 機能別ダッシュボード

| 機能 | 現在スコア（実測） | 既知の限界（正直リスト） | 次の改善候補 |
|---|---|---|---|
| **stem**（前処理・降噪） | SNR推定誤差 **±6dB以内**（既知SNR混合で固定）。降噪効果: 白色雑音SNR10dBでF1 0.118→1.000、ピンクSNR5dBで0→0.815 | 白色SNR5dBはpYIN検出消失（very_noisy報告に退避）。残響・実環境雑音の実録音検証は未実施（合成のみ） | 実録音フィールド素材での劣化カーブ計測。ノイズ種別の分類精度向上 |
| **ear**（音高検出） | ビブラート±0.5半音まで1音統合（境界ビブラートの20断片化を解消）。多声=Basic Pitch TFLite（無音でnote max 0.16=健全）。PDベンチ F1@100ms: rescue構成 0.650 / BP素点 0.709 | ビブラート±1.5半音で8断片前後（深いビブラートの構造限界）。rescue構成はF1でBP素点を下回る（score_rhythmでは上回る=トレードオフ）。ピッチベンド・グリッサンドの記譜方針未定義 | 感度の曲密度適応（密→high/疎→normal自動切替）。声のポルタメント対応 |
| **rhythm**（テンポ・量子化） | 合成13ケース テンポ誤差**±5%以内**。トルコ行進曲120.5・Romanze **96.0（誤差0%・三連格子）**。二重表現: raw側F1 0.678≈BP素点0.677（格子化ロス解消）・grid側score_rhythm 0.402>素点0.387。bpm不正入力は明示エラー・遅い側曖昧性は警告発火（偽警報ゼロ実測） | 速い側テンポ曖昧性（4分@200≡8分@100）は**数学的に検出不能**（証明つき）。三連の有効域96-120（72/90は2分系エイリアス）。ルバート（連続テンポ変化）未対応。一様音符列のエイリアシング | 区分定テンポ（テンポマップ）実装=ルバート対応。スウィング検出 |
| **notate**（綴り・譜面化） | 調推定 実曲2/2一致（a minor/e minor）。大譜表（加線3本以上 **0%**）・符幹100%・休符比率 real **13.5%**（旧46.9%）・三連誤爆0%。弱起=空小節シフト | 転調追従なし（曲全体1調 — realのAメジャー調号にAマイナー部が臨時記号だらけで載る）。左右手割当は高さ基準のみ（交差・跳躍で不自然になりうる）。声部分離は1声部+和音 | 区間調推定（転調追従）。声部分離（複旋律の独立表記）。アーティキュレーション記譜 |
| **engrave**（PDF描画） | Verovio 6.2.1・PD60秒→PDF 4.5秒・豆腐ゼロ（BPM平文表記）・XSD/往復/.mxl受入テスト固定・巨大/空/1音入力でクラッシュなし | テンポ記号は音符グリフでなく平文（フォント埋め込み未解決の回避策）。レイアウト微調整（詰まり）はVerovio既定任せ。LGPL依存の配布形態注意はADR-004記録 | SMuFLフォント解決で♩表記復活。ページレイアウトの調整パラメータ公開 |
| **quality**（AIの耳=審判） | 指標v2: オクターブ+12誤り **0.90→0.762＋offset検出**・幽霊大群 health素通り→**0.680＋issues表示**・score_rhythm総合組込（--reference時 重み0.35）。感度序列維持（同一0.888>改変>無関係）。58テスト | chroma/onsetは強弱・アーティキュレーション・声部正しさを測れない（台帳明記）。score_rhythmは正解MIDI必須（本番運用では使えない）。人間聴感との相関は未検証 | 正解なし運用向けの譜面健全性拡充。人間評価との相関キャリブレーション（ユーザーテスト時） |

## 第1巡の総括

- **P0 6件を全修正**（stem×2: SNR推定器の作り直し / ear×1: ビブラート断片化 / quality×3: オクターブ検出・health崖・score_rhythm重み）。P1群も #47/#49 で処理
- **攻撃テスト51件を新規固定**（#45-46: 17 / #47: 19 / #48: 9 / #49: 6）— 「全テスト緑のまま素通していた欠陥」を規約の未テスト入力攻撃で検出→修正→回帰固定のループが機能
- **全272テスト緑**（spike 214 + AIの耳 58）・ruffクリーン・テストを緩めた箇所ゼロ
- デモ再生成（全修正込み）: PD版 音符422・休符比率17.3%・三連0。実演奏版 音符1967・休符比率**13.5%（旧46.9%）**・5ページ・調号つき大譜表。豆腐ゼロ

## 第2巡について

ユーザー指示により、修正完了後の機能別ダメ出し第2巡を実施予定（規約の最大2巡目）。本ダッシュボードの「既知の限界」が第2巡の攻撃起点になる。
