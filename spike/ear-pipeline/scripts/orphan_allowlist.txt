# 孤立エクスポート allowlist (債務凍結ベースライン)
#
# ここに列挙したシンボルは __all__ にあるが実採譜フロー(pipeline.py)から未配線。
# #109 で 85件 → **0件** まで完全消化(2026-07-21):
#   - B-2a: 移動ド/度数/Nashville を `--analysis`
#   - B-2 : 汎用エミッタ(自動発見レジストリ)で29モジュールを `--emit`
#   - #113: gp5 を `--format` に再結線
#   - 残subcommand型: chunk / diff / compare を CLI サブコマンド化
#
# 現在この allowlist は **空**(凍結中の孤立0)。空ベースラインの意味:
# 新たに __all__ へ足したシンボルが pipeline から未到達なら即 CI fail する
# (check_orphan_exports.py)。結線せず意図的に保留する場合のみ、理由コメント付きで
# 1行1シンボルを追記すること。
#
# 追跡Issue: #109(結線・完了) / #111(本ゲート)
#
# 形式: 1行1シンボル。# 以降はコメント。
